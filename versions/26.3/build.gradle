// Generated shape: the plugin id selects the build family for this target.
// Everything else comes from versions.json via the shared script below.
plugins {
    id 'net.fabricmc.fabric-loom'
}

apply from: rootProject.file('gradle/version-module.gradle')
